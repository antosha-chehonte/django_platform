# План разработки приложения для учёта доступа сотрудников

## 📋 Общая концепция

Добавление функционала для учёта:
1. **Доступа к информационным системам** организации
2. **Цифровой подписи (электронной подписи)** сотрудников

### Архитектурные решения

- **Доступ к информационным системам**: одна модель `SystemAccess` для всех систем
- **Цифровая подпись**: отдельная модель `DigitalSignature` (отличается параметрами)

---

## ✅ Уточненные требования

### 1. Доступ к информационным системам (SystemAccess)

**Определенные параметры:**
- Система (ForeignKey к ITAsset)
- Логин (CharField)
- Статус (CharField с choices) - заменяет Boolean "актуальность"
- Дата получения доступа (DateField)
- Дата блокировки (DateField, null=True)
- Комментарии/примечания (TextField, blank=True)

**Статусы доступа:**
- `active` - Активен
- `suspended` - Приостановлен
- `blocked` - Заблокирован
- `deleted` - Удален
- `needs_update` - Требует актуализации

**Правила:**
- Один сотрудник может иметь несколько доступов к одной системе (разные логины = разные записи)
- Автоматическая установка статуса "требует актуализации" при увольнении или перемещении сотрудника
- Пароли не хранятся в системе
- История изменений не требуется

### 2. Цифровая подпись (DigitalSignature)

**Определенные параметры:**
- Сотрудник (ForeignKey к Employees)
- Тип сертификата (ForeignKey к CertificateType - отдельная модель)
- Серийный номер сертификата (CharField)
- Отпечаток сертификата (certificate_alias) (CharField)
- Дата окончания срока действия сертификата (DateField)
- Серийный номер носителя/токена (CharField, blank=True, null=True)
- Файл сертификата (FileField) - с возможностью скачать, форматы .cer, .pfx, максимум 1 МБ
- Статус (CharField с choices)
- Комментарии/примечания (TextField, blank=True)

**Статусы подписи:**
- `active` - Активна
- `revoked` - Аннулирована
- `needs_update` - Требует актуализации

**Правила:**
- Учитываются только КЭП (квалифицированная электронная подпись)
- Один сотрудник может иметь несколько активных подписей одновременно
- Автоматическая установка статуса "требует актуализации" при увольнении или перемещении сотрудника

**Модель CertificateType (справочник типов сертификатов):**
- Название типа (CharField)
- Описание (TextField, blank=True)
- Активность (BooleanField)

### 3. Общие требования

**Структура приложения:**
- Новое Django-приложение `apps.access_management`

**Права доступа:**
- Все авторизованные пользователи имеют одинаковые права, работать с учетом может только авторизованный пользователь.

**Функционал:**
- Отдельный раздел в меню для управления доступами
- Фильтры и поиск (по сотруднику, по системе, по статусу)
- Интеграция с профилем сотрудника: отображение доступов и подписей в детальной карточке
- Отчёты (после создания основы):
  - Список всех активных доступов к системе X (с группировкой по подразделениям)
  - Список всех сотрудников с активной цифровой подписью (с группировкой по подразделениям)
  - Список сотрудников без подписи (с группировкой по подразделениям)
  - Отчёты о просроченных/истекающих доступах и требующих актуализации статуса
- Массовый импорт из CSV (реализовать позже)

**Не требуется:**
- Уведомления
- История изменений
- Хранение паролей
- Центр сертификации

---

## ❓ Оставшиеся вопросы для уточнения

1. ✅ **Цифровая подпись - название сертификата:**
   - Решено: Не нужно хранить название/наименование сертификата.
   - Решено: Нужно хранить его отпечаток (alias) - добавлено поле `certificate_alias`.

2. ✅ **Валидация данных:**
   - Решено: Нужна валидация: дата блокировки должна быть >= даты получения доступа - добавлена в модель.
   - Решено: Дата выдачи сертификата исключена из модели, валидация не требуется.

3. ⚠️ **Автоматизация:**
   - Реализовано через сигналы Django (`post_save`, `post_delete`) для моделей `Employees` и `Posts` - это стандартный подход Django для такой задачи.

4. ✅ **Файлы сертификатов:**
   - Решено: Сохранять файлы сертификатов в директорию `media/certificates/` (настройки MEDIA добавлены в план)
   - Решено: Форматы файлов: .cer, .pfx
   - Решено: Ограничение размера файла: 1 МБ (валидатор добавлен в модель)

5. ✅ **Фильтры в списках:**
   - Решено: Для отчётов о просроченных/истекающих доступах - период "истекающим" считать в течение 40 дней.

---

## 🎯 Предварительный план реализации

### Этап 1: Создание структуры приложения

1. **Создать новое Django-приложение:**
   ```
   apps/access_management/
   ├── __init__.py
   ├── apps.py
   ├── admin.py
   ├── models.py
   ├── forms.py
   ├── views.py
   ├── urls.py
   ├── migrations/
   └── templates/
   ```

2. **Добавить приложение в INSTALLED_APPS:**
   - Добавить `apps.access_management` в `core/settings.py`

### Этап 2: Модели данных

#### 2.1 Модель CertificateType (Справочник типов сертификатов)

```python
class CertificateType(models.Model):
    """Справочник типов сертификатов цифровой подписи"""
    
    name = models.CharField(max_length=200, verbose_name="Название типа")
    description = models.TextField(blank=True, verbose_name="Описание")
    is_active = models.BooleanField(default=True, verbose_name="Активен")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="Дата создания")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="Дата обновления")
    
    class Meta:
        verbose_name = "Тип сертификата"
        verbose_name_plural = "Типы сертификатов"
        ordering = ['name']
    
    def __str__(self):
        return self.name
```

#### 2.2 Модель SystemAccess (Доступ к информационным системам)

```python
class SystemAccess(models.Model):
    """Доступ сотрудника к информационной системе"""
    
    STATUS_ACTIVE = 'active'
    STATUS_SUSPENDED = 'suspended'
    STATUS_BLOCKED = 'blocked'
    STATUS_DELETED = 'deleted'
    STATUS_NEEDS_UPDATE = 'needs_update'
    
    STATUS_CHOICES = (
        (STATUS_ACTIVE, 'Активен'),
        (STATUS_SUSPENDED, 'Приостановлен'),
        (STATUS_BLOCKED, 'Заблокирован'),
        (STATUS_DELETED, 'Удален'),
        (STATUS_NEEDS_UPDATE, 'Требует актуализации'),
    )
    
    employee = models.ForeignKey(
        'hr.Employees',
        on_delete=models.CASCADE,
        verbose_name='Сотрудник'
    )
    system = models.ForeignKey(
        'reference.ITAsset',
        on_delete=models.PROTECT,
        verbose_name='Система'
    )
    login = models.CharField(max_length=200, verbose_name='Логин')
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default=STATUS_ACTIVE,
        verbose_name='Статус'
    )
    access_granted_date = models.DateField(verbose_name='Дата получения доступа')
    access_blocked_date = models.DateField(
        null=True,
        blank=True,
        verbose_name='Дата блокировки'
    )
    notes = models.TextField(blank=True, verbose_name='Примечания')
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='Дата создания')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='Дата обновления')
    
    class Meta:
        verbose_name = 'Доступ к системе'
        verbose_name_plural = 'Доступы к системам'
        ordering = ['-created_at']
    
    def __str__(self):
        return f"{self.employee} - {self.system} ({self.login})"
    
    def clean(self):
        """Валидация данных"""
        super().clean()
        # Валидация даты блокировки (если указана)
        if self.access_blocked_date and self.access_granted_date:
            if self.access_blocked_date < self.access_granted_date:
                raise ValidationError({
                    'access_blocked_date': 'Дата блокировки не может быть раньше даты получения доступа.'
                })
```

#### 2.3 Модель DigitalSignature (Цифровая подпись)

```python
from django.core.validators import FileExtensionValidator
from django.core.exceptions import ValidationError

# Валидатор для ограничения размера файла (1 МБ)
class MaxFileSizeValidator:
    """Валидатор для ограничения размера файла (1 МБ)"""
    def __init__(self, max_size=1024 * 1024):  # 1 МБ по умолчанию
        self.max_size = max_size
    
    def __call__(self, value):
        if value.size > self.max_size:
            raise ValidationError(
                f'Размер файла не должен превышать {self.max_size / (1024 * 1024):.1f} МБ'
            )

class DigitalSignature(models.Model):
    """Цифровая подпись сотрудника"""
    
    STATUS_ACTIVE = 'active'
    STATUS_REVOKED = 'revoked'
    STATUS_NEEDS_UPDATE = 'needs_update'
    
    STATUS_CHOICES = (
        (STATUS_ACTIVE, 'Активна'),
        (STATUS_REVOKED, 'Аннулирована'),
        (STATUS_NEEDS_UPDATE, 'Требует актуализации'),
    )
    
    employee = models.ForeignKey(
        'hr.Employees',
        on_delete=models.CASCADE,
        verbose_name='Сотрудник'
    )
    certificate_type = models.ForeignKey(
        'access_management.CertificateType',
        on_delete=models.PROTECT,
        verbose_name='Тип сертификата'
    )
    certificate_serial = models.CharField(
        max_length=200,
        verbose_name='Серийный номер сертификата'
    )
    certificate_alias = models.CharField(
        max_length=200,
        verbose_name='Отпечаток сертификата (alias)'
    )
    expiry_date = models.DateField(verbose_name='Дата окончания срока действия')
    carrier_serial = models.CharField(
        max_length=200,
        blank=True,
        null=True,
        verbose_name='Серийный номер носителя (токен)'
    )
    certificate_file = models.FileField(
        upload_to='certificates/',
        verbose_name='Файл сертификата',
        validators=[
            FileExtensionValidator(allowed_extensions=['cer', 'pfx']),
            MaxFileSizeValidator(max_size=1024 * 1024)  # 1 МБ
        ],
        help_text='Форматы: .cer, .pfx. Максимальный размер: 1 МБ'
    )
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default=STATUS_ACTIVE,
        verbose_name='Статус'
    )
    notes = models.TextField(blank=True, verbose_name='Примечания')
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='Дата создания')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='Дата обновления')
    
    class Meta:
        verbose_name = 'Цифровая подпись'
        verbose_name_plural = 'Цифровые подписи'
        ordering = ['-created_at']
    
    def __str__(self):
        return f"{self.employee} - {self.certificate_serial}"
    
    def clean(self):
        """Валидация данных"""
        super().clean()
        # Валидация даты окончания (должна быть в будущем или не слишком далеко)
        from django.utils import timezone
        today = timezone.now().date()
        if self.expiry_date < today:
            # Сертификат уже истек - это допустимо, но можно предупредить
            pass
    
    @property
    def is_expired(self):
        """Проверка истечения срока действия"""
        from django.utils import timezone
        return self.expiry_date < timezone.now().date()
```

### Этап 2.4: Сигналы для автоматического обновления статусов

**Задача:** Автоматически устанавливать статус "требует актуализации" при увольнении или перемещении сотрудника.

**Подход:**
- Использовать сигналы Django (`post_save`, `post_delete`)
- Отслеживать изменения в модели `Employees` (поле `is_active`)
- Отслеживать изменения в модели `Posts` (изменение связи сотрудник-должность)

```python
# apps/access_management/signals.py

@receiver(post_save, sender=Employees)
def update_access_on_employee_change(sender, instance, **kwargs):
    """Обновление статуса доступов при изменении статуса сотрудника"""
    if not instance.is_active:
        SystemAccess.objects.filter(
            employee=instance,
            status__in=[SystemAccess.STATUS_ACTIVE, SystemAccess.STATUS_SUSPENDED]
        ).update(status=SystemAccess.STATUS_NEEDS_UPDATE)
        
        DigitalSignature.objects.filter(
            employee=instance,
            status=DigitalSignature.STATUS_ACTIVE
        ).update(status=DigitalSignature.STATUS_NEEDS_UPDATE)

@receiver(post_save, sender=Posts)
@receiver(post_delete, sender=Posts)
def update_access_on_post_change(sender, instance, **kwargs):
    """Обновление статуса доступов при изменении должности сотрудника"""
    if instance.employee:
        SystemAccess.objects.filter(
            employee=instance.employee,
            status__in=[SystemAccess.STATUS_ACTIVE, SystemAccess.STATUS_SUSPENDED]
        ).update(status=SystemAccess.STATUS_NEEDS_UPDATE)
        
        DigitalSignature.objects.filter(
            employee=instance.employee,
            status=DigitalSignature.STATUS_ACTIVE
        ).update(status=DigitalSignature.STATUS_NEEDS_UPDATE)
```

### Этап 3: Административный интерфейс (Django Admin)

1. **Настроить admin.py:**
   - Зарегистрировать модели `CertificateType`, `SystemAccess` и `DigitalSignature`
   - Настроить list_display, list_filter, search_fields
   - Добавить действия для массового изменения статусов
   - Настроить отображение файлов сертификатов с возможностью скачивания

2. **Формы:**
   - Создать формы для создания/редактирования доступа (`SystemAccessForm`)
   - Создать формы для создания/редактирования подписи (`DigitalSignatureForm`)
   - Добавить валидацию в формах (даты, обязательные поля)

### Этап 4: Представления и URL

1. **Views:**
   - Список всех доступов к системам
   - Детальная информация о доступе
   - Создание/редактирование/удаление доступа
   - Список всех цифровых подписей
   - Детальная информация о подписи
   - Создание/редактирование/удаление подписи

2. **URL маршруты:**
   ```
   /access/systems/ - список доступов
   /access/systems/<id>/ - детали доступа
   /access/signatures/ - список подписей
   /access/signatures/<id>/ - детали подписи
   ```

### Этап 5: Интеграция с профилем сотрудника

1. **В карточке сотрудника (`apps/hr/views.py`):**
   - Добавить отображение списка доступов сотрудника
   - Добавить отображение цифровых подписей сотрудника
   - Добавить ссылки на управление доступами

2. **В шаблоне детальной карточки сотрудника:**
   - Добавить секцию "Доступы к системам"
   - Добавить секцию "Цифровые подписи"

### Этап 6: Шаблоны (Templates)

1. **Создать базовый шаблон:**
   ```
   templates/access_management/base.html
   ```

2. **Создать шаблоны для списков:**
   - `system_access_list.html`
   - `digital_signature_list.html`

3. **Создать шаблоны для детальной информации:**
   - `system_access_detail.html`
   - `digital_signature_detail.html`

4. **Создать шаблоны для форм:**
   - `system_access_form.html`
   - `digital_signature_form.html`

### Этап 7: Фильтры и поиск

1. **Для списка доступов:**
   - Фильтр по сотруднику (через Q-объекты)
   - Фильтр по системе (ITAsset)
   - Фильтр по статусу (выбор из всех статусов)
   - Поиск по логину, ФИО сотрудника, названию системы

2. **Для списка подписей:**
   - Фильтр по сотруднику
   - Фильтр по статусу (выбор из всех статусов)
   - Фильтр по типу сертификата (CertificateType)
   - Фильтр по истекающим срокам (в течение 40 дней)
   - Поиск по серийному номеру сертификата, отпечатку (certificate_alias), серийному номеру токена, ФИО сотрудника

### Этап 8: Миграции базы данных

1. Создать миграции для моделей
2. Применить миграции

### Этап 9: Тестирование: создавать тесты не требуется

### Этап 10: Скачивание файлов сертификатов

1. **View для скачивания файла:**
   - Создать представление для безопасного скачивания файла сертификата
   - Проверка прав доступа
   - Логирование факта скачивания (опционально)

2. **URL для скачивания:**
   - `/access/signatures/<id>/download/` - скачать файл сертификата

### Этап 11: Отчёты (реализовать после основы)

1. **Отчёты для доступа к системам:**
   - Список всех активных доступов к системе X с группировкой по подразделениям
   - Отчёт о просроченных/истекающих доступах (требующих актуализации)

2. **Отчёты для цифровых подписей:**
   - Список всех сотрудников с активной цифровой подписью (с группировкой по подразделениям)
   - Список сотрудников без подписи (с группировкой по подразделениям)
   - Отчёт об истекающих сертификатах

3. **Экспорт:**
   - Экспорт в Excel/CSV
   - Экспорт в PDF (опционально)

### Этап 12: Массовый импорт (реализовать позже)

1. **Импорт доступов:**
   - Массовый импорт из CSV
   - Валидация данных при импорте
   - Обработка ошибок

2. **Импорт подписей:**
   - Массовый импорт из CSV
   - Загрузка файлов сертификатов при импорте
   - Валидация данных при импорте

---

## 📝 Зависимости от оставшихся вопросов

### Высокоприоритетные вопросы (влияют на структуру моделей):

1. ✅ **Название сертификата:** Решено - не нужно отдельное поле для названия, добавлено поле `certificate_alias` (отпечаток).
2. ✅ **Валидация дат:** Решено - валидация для даты блокировки добавлена, дата выдачи исключена из модели.
3. ✅ **Файлы сертификатов:** Решено - путь: `media/certificates/`, форматы: .cer, .pfx, размер: 1 МБ.

### Среднеприоритетные вопросы (влияют на функционал):

1. ⚠️ **Автоматизация статусов:** реализовано через сигналы Django, срабатывает при изменении `is_active` сотрудника и при изменении связи с должностью (Posts).
2. ✅ **Фильтры:** Решено - период для "истекающих" сертификатов/доступов: в течение 40 дней.

### Решенные вопросы (✅):

1. ✅ Параметры модели `DigitalSignature` - определены (исключена дата выдачи, добавлен отпечаток)
2. ✅ История изменений - не нужна (`AccessHistory` удалена из плана)
3. ✅ Файлы сертификатов - нужно хранить с возможностью скачивания, форматы .cer, .pfx, размер до 1 МБ
4. ✅ Правила уникальности - разрешены множественные доступы/подписи
5. ✅ Уведомления - не нужны
6. ✅ Отчёты - определены, реализовать после основы
7. ✅ Массовая загрузка - определена, реализовать позже
8. ✅ Отпечаток сертификата - добавлено поле `certificate_alias`

---

## 🗂️ Структура файлов после реализации

```
apps/
└── access_management/
    ├── __init__.py
    ├── apps.py
    ├── admin.py
    ├── models.py          # CertificateType, SystemAccess, DigitalSignature
    ├── forms.py           # SystemAccessForm, DigitalSignatureForm, CertificateTypeForm
    ├── views.py           # CRUD для доступов и подписей, скачивание файлов
    ├── urls.py            # Маршруты
    ├── signals.py         # Сигналы для автоматического обновления статусов
    ├── migrations/
    │   └── 0001_initial.py
    ├── tests.py
    └── templates/
        └── access_management/
            ├── base.html
            ├── system_access_list.html
            ├── system_access_detail.html
            ├── system_access_form.html
            ├── digital_signature_list.html
            ├── digital_signature_detail.html
            ├── digital_signature_form.html
            └── certificate_type_list.html  # Для управления типами сертификатов

templates/hr/
└── employee_detail.html  # Обновить: добавить секции с доступами
```

---


## 🔧 Технические детали реализации

### Настройка MEDIA для файлов сертификатов

В `core/settings.py`:
```python
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

В `core/urls.py` (для разработки):
```python
from django.conf import settings
from django.conf.urls.static import static

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

### Подключение сигналов

В `apps/access_management/apps.py`:
```python
class AccessManagementConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.access_management'
    
    def ready(self):
        import apps.access_management.signals  # noqa
```

