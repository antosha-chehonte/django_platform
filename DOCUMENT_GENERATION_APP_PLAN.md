# План разработки приложения для генерации документов

## 📊 Оценка задачи и предложенных технологий

### Общая оценка задачи

**Цель:** Создать Django-приложение для автоматической генерации документов из шаблонов ODT с подстановкой данных сотрудников для массовой генерации документов по списку сотрудников.

**Сложность:** Средняя. Задача требует интеграции внешних библиотек, работы с шаблонами документов и обработки лингвистических данных.

**Ключевые требования:**
- Использование шаблонов ODT с Jinja2-синтаксисом
- Подстановка ФИО сотрудника в винительном падеже (хранится в БД)
- Подстановка даты назначения и сведений о приказе
- Подстановка должности в соответствующем падеже из модели `Posts`
- Интеграция с существующей моделью `Employees` из приложения `hr`
- Массовая генерация документов для списка сотрудников

### Оценка технологий

#### ✅ PythonODFTemplator

**Преимущества:**
- Прямая поддержка Jinja2-синтаксиса (знакомый многим разработчикам)
- Работа с открытым форматом ODT (LibreOffice/OpenOffice)
- Простая интеграция с Django
- Гибкость шаблонов (можно использовать условные конструкции, циклы)

**Потенциальные проблемы:**
- Необходимо проверить актуальность библиотеки и совместимость с текущими версиями Python
- Возможные ограничения при работе со сложным форматированием ODT
- Требуется тестирование на различных шаблонах

**Рекомендация:** Использовать, предварительно проверив актуальность и функциональность библиотеки.

#### ✅ pytrovich

**Преимущества:**
- Специализированная библиотека для склонения русских имен
- Поддержка различных падежей, включая винительный
- Учитывает пол сотрудника (данные уже есть в модели `Employees`)
- Обработка стандартных случаев склонения

**Потенциальные проблемы:**
- Может не справляться с редкими или нестандартными именами
- Возможны ошибки при отсутствии отчества
- Требуется обработка исключений и fallback-механизм
- Необходимо тестирование на реальных данных сотрудников

**Рекомендация:** Использовать с реализацией механизма обработки ошибок и возможности ручной корректировки.

---

## 📋 План разработки приложения

### Этап 1: Исследование и подготовка

#### 1.1. Проверка и установка зависимостей
- [ ] Проверить актуальность библиотеки `PythonODFTemplator` (PyPI, GitHub)
- [ ] Проверить актуальность библиотеки `pytrovich` (PyPI, GitHub)
- [ ] Определить совместимость с текущей версией Python проекта
- [ ] Добавить библиотеки в `requirements.txt` (использовать стабильные версии)
- [ ] Установить и протестировать базовую функциональность

**Принятые решения:**
- ✅ Использовать стабильные версии библиотек
- ✅ Альтернативные библиотеки не рассматриваются
- ✅ При ошибках склонения - оставлять поле пустым для ручной обработки

#### 1.2. Анализ структуры данных
- [ ] Изучить модель `Employees` и доступные поля
- [ ] Изучить модель `Posts` для получения должности сотрудника
- [ ] Определить, какие данные будут использоваться в шаблонах
- [ ] Проверить полноту данных (могут ли быть пустыми `appointment_date`, `appointment_order_number`)

**Принятые решения:**
- ✅ Для документов необходима должность в соответствующем падеже из модели `Posts`
- ✅ Нужна связь с моделью `Posts` для получения актуальной должности сотрудника
- ✅ Случаи без отчества (`middle_name` пустое) - поле отчества остается пустым, ошибки обрабатываются вручную
- ✅ Формат даты в документах: "15.01.2024"

### Этап 2: Расширение модели Employees

#### 2.1. Добавление полей для хранения склоненных ФИО
- [ ] Создать миграцию для добавления полей в модель `Employees`:
  ```python
  last_name_accusative = models.CharField(max_length=150, blank=True, verbose_name='Фамилия (винительный)')
  first_name_accusative = models.CharField(max_length=150, blank=True, verbose_name='Имя (винительный)')
  middle_name_accusative = models.CharField(max_length=150, blank=True, verbose_name='Отчество (винительный)')
  ```
- [ ] Добавить метод для получения полного ФИО в винительном падеже:
  ```python
  def get_full_name_accusative(self) -> str:
      """Возвращает ФИО в винительном падеже"""
  ```
- [ ] Добавить поля в `admin.py` и формы для редактирования

**Преимущества подхода:**
- ✅ ФИО в винительном падеже генерируется один раз при создании/изменении сотрудника
- ✅ Возможность ручной проверки и корректировки при создании сотрудника
- ✅ Быстрая генерация документов для списка сотрудников (нет необходимости склонять каждый раз)
- ✅ Хранение исправленных вручную данных

### Этап 3: Проектирование архитектуры приложения

#### 3.1. Структура приложения
- [ ] Создать новое Django-приложение `apps.document_generation`
- [ ] Определить структуру папок:
  - `models.py` - не требуется (модели не используются)
  - `utils/` - утилиты для генерации документов
    - `name_declension.py` - функции склонения ФИО
    - `date_formatter.py` - функции форматирования дат
    - `document_generator.py` - основная логика генерации документов
  - `forms.py` - формы для выбора сотрудников и шаблона
  - `views.py` - представления для генерации документов
  - `signals.py` - сигналы для автоматического склонения ФИО при создании/обновлении сотрудника
  - `templates/` - HTML-шаблоны для интерфейса
  - `templates_documents/` - папка для хранения ODT-шаблонов (в файловой системе)
  - `urls.py` - маршруты приложения

**Принятые решения:**
- ✅ Хранить шаблоны ODT в файловой системе
- ✅ Не нужна модель для хранения метаданных о шаблонах
- ✅ Не нужна система категорий/типов документов
- ✅ Права доступа: только авторизованные пользователи
- ✅ Не нужна версионность шаблонов
- ✅ Не нужна история использования шаблонов
- ✅ Не нужна возможность предпросмотра шаблона перед генерацией

### Этап 4: Разработка модуля склонения ФИО

#### 4.1. Создание сервиса склонения
- [ ] Создать модуль `apps/document_generation/utils/name_declension.py`
- [ ] Реализовать функции склонения отдельных частей ФИО:
  ```python
  def decline_last_name(last_name: str, gender: str) -> str:
      """Склоняет фамилию в винительный падеж"""
  
  def decline_first_name(first_name: str, gender: str) -> str:
      """Склоняет имя в винительный падеж"""
  
  def decline_middle_name(middle_name: str, gender: str) -> str:
      """Склоняет отчество в винительный падеж"""
  
  def generate_full_name_accusative(employee: Employees) -> tuple[str, str, str]:
      """
      Генерирует ФИО в винительном падеже.
      Возвращает кортеж (last_name_accusative, first_name_accusative, middle_name_accusative).
      При ошибках склонения возвращает исходные значения.
      """
  ```

**Принятые решения:**
- ✅ Ошибки склонения - возвращать исходное ФИО (пользователь может исправить вручную)
- ✅ Возможность ручной корректировки склоненного ФИО в форме создания/редактирования сотрудника
- ✅ Случаи с отсутствующим отчеством - поле отчества остается пустым
- ✅ Кэширование не требуется (склонение происходит только при создании/изменении сотрудника)

#### 4.2. Интеграция с моделью Employees через сигналы
- [ ] Создать модуль `apps/document_generation/signals.py`
- [ ] Реализовать сигналы для автоматического склонения ФИО:
  ```python
  from django.db.models.signals import pre_save, post_save
  from apps.hr.models import Employees
  
  @receiver(pre_save, sender=Employees)
  def generate_accusative_names(sender, instance, **kwargs):
      """
      Автоматически генерирует ФИО в винительном падеже при сохранении сотрудника.
      Выполняется только если поля склонения пусты или изменились исходные ФИО.
      """
  ```
- [ ] Зарегистрировать сигналы в `apps.py` приложения `document_generation`

**Логика работы:**
- При создании нового сотрудника автоматически генерируются склоненные ФИО
- При изменении `last_name`, `first_name`, `middle_name` или `gender` - пересчитываются склоненные ФИО
- Пользователь может вручную исправить сгенерированные значения в форме редактирования
- Метод `get_full_name_accusative()` в модели `Employees` возвращает склоненное ФИО из сохраненных полей

### Этап 5: Разработка модуля генерации документов

#### 5.1. Создание сервиса генерации
- [ ] Создать модуль `apps/document_generation/utils/document_generator.py`
- [ ] Реализовать функцию генерации документа:
  ```python
  def generate_document(template_path: str, employee: Employees) -> bytes:
      """
      Генерирует документ из шаблона ODT с подстановкой данных сотрудника.
      Возвращает содержимое файла в формате ODT в виде bytes.
      """
  ```
- [ ] Реализовать функцию массовой генерации:
  ```python
  def generate_documents_batch(template_path: str, employees: QuerySet[Employees]) -> list[tuple[Employees, bytes]]:
      """
      Генерирует документы для списка сотрудников.
      Возвращает список кортежей (employee, document_bytes).
      """
  ```

**Принятые решения:**
- ✅ Формат возврата: `bytes` для `HttpResponse` (для скачивания)
- ✅ Сгенерированные документы не сохраняются на диск (только в памяти для скачивания)
- ✅ Генерация в других форматах (PDF, DOCX) не требуется на первом этапе
- ✅ Ошибки генерации обрабатываются через исключения с понятными сообщениями для пользователя

#### 5.2. Получение данных о должности сотрудника
- [ ] Создать функцию для получения текущей должности сотрудника:
  ```python
  def get_employee_post(employee: Employees) -> Posts | None:
      """Возвращает текущую должность сотрудника из модели Posts"""
  
  def get_post_name_accusative(employee: Employees) -> str:
      """Возвращает название должности в винительном падеже"""
  ```
- [ ] Изучить модель `Postname` для понимания структуры названий должностей

**Ответы на вопросы для разрешения:**
- ❓ Названия должностей в винительном падеже будем хранить в модели должностей Postname и заполнять при создании новой должности.
- ❓ Поскольку список для генерации документов будет формироваться на основе занятых должностей, то ситуации, когда у сотрудника нет текущей должности в `Posts` быть не должно

#### 5.3. Подготовка контекста для шаблона
- [ ] Определить структуру контекста для Jinja2:
  ```python
  def prepare_template_context(employee: Employees) -> dict:
      """
      Подготавливает контекст для шаблона документа.
      """
      post = get_employee_post(employee)
      return {
          'employee_full_name_accusative': employee.get_full_name_accusative(),
          'appointment_date': employee.appointment_date,
          'appointment_date_formatted': format_date(employee.appointment_date),
          'appointment_order_date': employee.appointment_order_date,
          'appointment_order_date_formatted': format_date(employee.appointment_order_date) if employee.appointment_order_date else '',
          'appointment_order_number': employee.appointment_order_number,
          'post_name': post.postname.name if post else '',
          'post_name_accusative': get_post_name_accusative(employee),
          'department_name': post.department.name if post else '',
      }
  ```

**Принятые решения:**
- ✅ Использовать данные из модели `Posts` для получения должности и подразделения
- ✅ Пустые значения обрабатываются через проверки в шаблоне Jinja2 (`{% if %}`)

#### 5.4. Форматирование дат
- [ ] Создать модуль `apps/document_generation/utils/date_formatter.py`
- [ ] Реализовать функцию форматирования дат:
  ```python
  def format_date(date: date | None, format_type: str = 'short') -> str:
      """
      Форматирует дату для использования в документах.
      'short': "15.01.2024"
      """
      if date is None:
          return ''
      if format_type == 'short':
          return date.strftime('%d.%m.%Y')
      return str(date)
  ```

**Принятые решения:**
- ✅ Формат даты: "15.01.2024" (короткий формат)
- ✅ Поддержка разных форматов не требуется на первом этапе

### Этап 6: Разработка пользовательского интерфейса

#### 6.1. Отдельной формы для выбора сотрудников для генерации документов не требуются, так как выбор сотрудников будет происходить из другого приложения по учету Электронных подписей: как правило - отфильтровать сотрудников по дате истечения срока действия подписи и получившийся список отправить на генерацию.

**Принятые решения:**
- ✅ Фильтрация сотрудников: только активные (`is_active=True`)
- ✅ Массовая генерация документов для нескольких сотрудников - основная функциональность
- ✅ Предпросмотр данных не требуется

#### 6.2. Представления (Views)
- [ ] Создать `DocumentGenerationView` (FormView):
  ```python
  class DocumentGenerationView(LoginRequiredMixin, FormView):
      form_class = DocumentGenerationForm
      template_name = 'document_generation/generate_form.html'
      
      def form_valid(self, form):
          # Генерация документов для выбранных сотрудников
          # Возврат ZIP-архива с документами
  ```
- [ ] Реализовать генерацию ZIP-архива с документами для нескольких сотрудников
- [ ] Реализовать скачивание одного документа или ZIP-архива

**Принятые решения:**
- ✅ Название файлов: автоматически на основе шаблона и ФИО сотрудника
- ✅ Страница со списком шаблонов не требуется (выбор из выпадающего списка)
- ✅ История сгенерированных документов не требуется

#### 6.3. HTML-шаблоны
- [ ] Создать шаблон `templates/document_generation/generate_form.html`
- [ ] Создать базовый шаблон `templates/document_generation/base.html` (если нужно)
- [ ] Интегрировать с существующим дизайном проекта (использовать `base.html`)

**Принятые решения:**
- ✅ Страница со списком шаблонов не требуется
- ✅ Загрузка новых шаблонов через интерфейс не требуется (шаблоны добавляются в файловую систему администратором)

### Этап 7: Создание примерного ODT-шаблона

#### 7.1. Подготовка шаблона
- [ ] Создать примерный ODT-шаблон в `apps/document_generation/templates_documents/example.odt`
- [ ] Протестировать подстановку переменных:
  - `{{ employee_full_name_accusative }}`
  - `{{ appointment_date_formatted }}`
  - `{{ appointment_order_number }}`
  - `{{ appointment_order_date_formatted }}`
  - `{{ post_name_accusative }}`
  - `{{ department_name }}`
- [ ] Протестировать обработку пустых значений через `{% if %}`

**Принятые решения:**
- ✅ Синтаксис Jinja2 определяется библиотекой PythonODFTemplator (стандартный синтаксис)
- ✅ Форматирование в документе сохраняется из шаблона ODT (жирный шрифт, выравнивание и т.д.)
- ✅ Примеры разных типов документов не требуются на первом этапе

### Этап 8: Интеграция с проектом

#### 8.1. Настройка Django
- [ ] Добавить приложение в `INSTALLED_APPS` в `core/settings.py`:
  ```python
  INSTALLED_APPS = [
      # ...
      'apps.document_generation',
  ]
  ```
- [ ] Добавить маршруты в `core/urls.py`:
  ```python
  path('documents/', include(('apps.document_generation.urls', 'documents'), namespace='documents')),
  ```
- [ ] Создать файл `apps/document_generation/urls.py` с маршрутами
- [ ] Настроить путь к папке с шаблонами в `settings.py` (опционально, можно использовать относительный путь)

**Принятые решения:**
- ✅ Namespace для URL: `documents`
- ✅ Права доступа: только авторизованные пользователи (через `LoginRequiredMixin`)

#### 8.2. Добавление в навигацию
- [ ] Добавить ссылку на приложение в главное меню (`apps/project_info/views.py`)
- [ ] Создать страницу-индекс приложения `DocumentGenerationIndexView`

### Этап 9: Тестирование не требуется

**Процесс проверки склонения:**
- При создании сотрудника пользователь видит сгенерированные склоненные ФИО в форме
- Пользователь может вручную исправить некорректные склонения
- Исправленные значения сохраняются в БД

### Этап 10: Документация и финализация

#### 10.1. Документация для пользователей 
- [ ] Создать инструкцию по использованию приложения (`DOCUMENT_GENERATION_USAGE.md`)
- [ ] Создать руководство по созданию шаблонов ODT (`DOCUMENT_TEMPLATES_GUIDE.md`)
- [ ] Описать доступные переменные для шаблонов:
  - `employee_full_name_accusative` - ФИО в винительном падеже
  - `appointment_date_formatted` - Дата назначения (формат: DD.MM.YYYY)
  - `appointment_order_number` - Номер приказа о назначении
  - `appointment_order_date_formatted` - Дата приказа о назначении (формат: DD.MM.YYYY)
  - `post_name_accusative` - Название должности в винительном падеже
  - `department_name` - Название подразделения

#### 10.2. Документация для разработчиков
- [ ] Задокументировать архитектуру приложения
- [ ] Описать API сервисов (docstrings)
- [ ] Добавить docstrings во все функции и классы
- [ ] Описать процесс автоматического склонения ФИО через сигналы

---

## 🔑 Вопросы, требующие разрешения в процессе разработки

### Технические вопросы

1. **Обработка ошибок при генерации:** я не знаю ответы на эти вопросы
   - ❓ Как обрабатывать ошибки при генерации (неправильный шаблон, отсутствие переменных)?
   - ❓ Как информировать пользователя об ошибках при массовой генерации? (показывать список сотрудников с ошибками?)

3. **Массовая генерация:**
   - ❓ Большие списки сотрудников (100+) обрабатываться не будут

### Вопросы по модели данных

4. **Миграция существующих данных:**
   - ❓ Заполнять поля `*_accusative` для существующих сотрудников в БД не требуется, база будет чистой.

---

## 🎯 Следующие шаги

1. **Немедленно:** 
   - Проверить актуальность и функциональность библиотек PythonODFTemplator и pytrovich
   - Изучить документацию pytrovich для понимания работы с отсутствующим отчеством

2. **Важно:** 
   - Определиться с подходом к склонению названий должностей
   - Решить вопрос о миграции существующих данных сотрудников

3. **Начать разработку:** 
   - Создать миграцию для добавления полей `*_accusative` в модель `Employees`
   - Реализовать модуль склонения ФИО и сигналы
   - Создать базовую структуру приложения `document_generation`
   - Реализовать прототип генерации одного документа

